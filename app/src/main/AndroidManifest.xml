<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/osm_ic_center_map"
        android:label="@string/app_name"
        android:roundIcon="@drawable/round_navigation_white_48"
        android:supportsRtl="true"
        android:theme="@style/Theme.SULITRIP"
        android:requestLegacyExternalStorage="true">

        <!-- Declare activities -->
        <activity android:name=".MapActivity"
            android:theme="@style/Theme.SULITRIP"/>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity"
            android:exported="true" android:theme="@style/Theme.AppCompat.DayNight">

            <!-- Launch MainActivity by default -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchActivity" />
        <activity android:name=".NavActivity" />
        <activity android:name=".SavedLocationActivity" />

    </application>


</manifest>